def <BUILTIN>:INTRINSICS.WebAssembly.compile(
  this: ESValue,
  ArgumentsList: List[ESValue],
  NewTarget: Record[Constructor] | Undefined,
): Unknown = {
  let __args__ = (record)
  if (< 0 (sizeof ArgumentsList)) {
    pop bytes < ArgumentsList
    expand __args__.bytes
  } else let bytes = undefined
  call %0 = clo<"IsBufferSource">(bytes)
  if (= %0 false) {
    %1 = @EXECUTION_STACK[0].Realm.Intrinsics["%Promise%"]
    call %2 = clo<"NewPromiseCapability">(%1)
    assert (? %2: Completion)
    if (? %2: Abrupt) return %2
    else %2 = %2.Value
    let promise = %2
    call %3 = clo<"__NEW_ERROR_OBJ__">("%TypeError.prototype%")
    call %4 = clo<"Call">(promise.Reject, undefined, (list [%3]))
    assert (? %4: Normal)
    let reject = promise.Promise
    call %5 = clo<"NormalCompletion">(reject)
    return %5
  }
  call %6 = clo<"compile_bytes_">(this, bytes)
  if (? %6: Completion) return %6
  call %7 = clo<"NormalCompletion">(%6)
  return %7
}
