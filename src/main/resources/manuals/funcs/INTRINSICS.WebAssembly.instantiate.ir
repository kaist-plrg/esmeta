def <BUILTIN>:INTRINSICS.WebAssembly.instantiate(
  this: ESValue,
  ArgumentsList: List[ESValue],
  NewTarget: Record[Constructor] | Undefined,
): Unknown = {
  let __args__ = (record)
  if (< 0 (sizeof ArgumentsList)) {
    pop arg < ArgumentsList
    expand __args__.arg
  } else let arg = undefined
  if (< 0 (sizeof ArgumentsList)) {
    pop importObject < ArgumentsList
    expand __args__.importObject
  } else let importObject = undefined
  call %0 = clo<"IsBufferSource">(arg)
  if (= %0 true) {
    call %1 = clo<"instantiate_bytes_importObject_">(this, arg, importObject)
    if (? %1: Completion) return %1
    call %2 = clo<"NormalCompletion">(%1)
    return %2
  }
  call %3 = clo<"IsModule">(arg)
  if (= %3 true) {
    call %4 = clo<"instantiate_moduleObject_importObject_">(this, arg, importObject)
    if (? %4: Completion) return %4
    call %5 = clo<"NormalCompletion">(%4)
    return %5
  }
  %6 = @EXECUTION_STACK[0].Realm.Intrinsics["%Promise%"]
  call %7 = clo<"NewPromiseCapability">(%6)
  assert (? %7: Completion)
  if (? %7: Abrupt) return %7
  else %7 = %7.Value
  let promise = %7
  call %8 = clo<"__NEW_ERROR_OBJ__">("%TypeError.prototype%")
  call %9 = clo<"Call">(promise.Reject, undefined, (list [%8]))
  assert (? %9: Normal)
  let reject = promise.Promise
  call %10 = clo<"NormalCompletion">(reject)
  return %10
}
