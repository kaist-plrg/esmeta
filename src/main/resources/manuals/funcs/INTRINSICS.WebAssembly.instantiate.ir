def <BUILTIN>:INTRINSICS.WebAssembly.instantiate(
  this: ESValue,
  ArgumentsList: List[ESValue],
  NewTarget: Record[Constructor] | Undefined,
): Unknown = {
  let __args__ = (record)
  if (< 0 (sizeof ArgumentsList)) {
    pop bytes < ArgumentsList
    expand __args__.bytes
  } else let bytes = undefined
  if (< 0 (sizeof ArgumentsList)) {
    pop importObject < ArgumentsList
    expand __args__.importObject
  } else let importObject = undefined
  if (< 0 (sizeof ArgumentsList)) {
    pop option < ArgumentsList
    expand __args__.option
  } else let options = (map[String, Unknown])
  call %0 = clo<"ToWebIDLArrayBuffer">(bytes)
  call %1 = clo<"instantiate">(%0, importObject, options)
  %2 = %1.Promise
  call %3 = clo<"NormalCompletion">(%2)
  return %3
}
